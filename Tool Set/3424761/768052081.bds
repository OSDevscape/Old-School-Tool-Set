$c[─────────────────────────────────────────────────────────────────────────────────────────────────────] 
$c[Old School Tool Set License v1.2 — © 2025 OSDevscape | All Rights Reserved]
$c[OSRS Wiki–derived text and media used here are licensed separately under CC BY-SA 3.0]
$c[For full license details, see: https://github.com/OSDevscape/Old-School-Tool-Set/blob/main/LICENSE]
$c[─────────────────────────────────────────────────────────────────────────────────────────────────────] 
$c[SPDX-License-Identifier: LicenseRef-OSDevscape-OSTS-1.2 AND CC-BY-SA-3.0]
$c[─────────────────────────────────────────────────────────────────────────────────────────────────────]

$nomention

$suppressErrors[
> The Hiscore Scribes stumbled while reading this profile.
> Please try again in a moment.
]

$c[Resolve target user ID]
$if[$message[who]==]
$var[target;$authorID]
$else
$var[target;$message[who]]
$endif

$c[Try WOM, but we will only *use* it if snapshot is valid]
$httpGet[https://api.wiseoldman.net/v2/players/$getVar[RSN;$var[target]]]

$c[Footer / base embed styling]
$thumbnail[$userAvatar[$var[target]]]
$footer[$addTimestamp]
$color[$getRoleColor[$highestRole[$var[target]]]]

$c[Base title uses RSN if we have one, otherwise the username]
$var[profileName;$getVar[RSN;$var[target]]]
$if[$var[profileName]==]
$var[profileName;$username[$var[target]]]
$endif

$title[__**$var[profileName]**__'s Profile]

$c[Start building description with server stats only]
$var[desc;
## Server Stats 
─────────────────────────
📊 **Level**: $getVar[level;$var[target]]
🪙 **GP**: $getVar[🪙;$var[target]]
🏦 **Bank Value**: $getVar[🏦;$var[target]]
☠️ **Completed Tasks**: $getVar[SlayerTasksCompleted;$var[target]]
💀 **Slayer Points**: $getVar[💀;$var[target]]
─────────────────────────
]

$c[If WOM returned a valid latest snapshot, append WOM info to the description]
$if[$and[$httpStatus==200;$httpResult[latestSnapshot;id]!=]]

$var[desc;
> **Build**: $httpResult[build]
> **Type**: $httpResult[type]
> **ID**: $httpResult[id]

<:Stats_icon:1426867260483702884> __**Totals (Hiscores)**__
> Level: $numberSeparator[$httpResult[latestSnapshot;data;skills;overall;level];,]
> XP: $numberSeparator[$httpResult[latestSnapshot;data;skills;overall;experience];,]

$var[desc]
]

$endif

$description[$var[desc]]